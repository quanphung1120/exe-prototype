# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/

# production builds
dist/
build/

# testing
coverage/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# …but keep the example templates tracked.
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma — generated client (regenerated via `prisma generate` / postinstall)
api/src/generated/

# scratch / superseded code kept locally, never committed
.temp/

# local pnpm store
.pnpm-store/
