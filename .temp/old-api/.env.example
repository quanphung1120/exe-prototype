# Copy to `.env` and fill in. The API reads these via `import "dotenv/config"`.

# ── Database (MongoDB / Mongoose) ────────────────────────────────────────────
# MongoDB connection string. Consumed by Mongoose in src/db.ts (`db:check`
# pings it). Use your Atlas SRV URI.
DATABASE_URL="mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/DBNAME?retryWrites=true&w=majority"

# ── Auth (Clerk) ─────────────────────────────────────────────────────────────
# The API is guarded by @clerk/hono: every /api/* request must carry a
# valid Clerk session token. These are the same keys the web app uses (copy
# them from apps/web/.env.local).
CLERK_PUBLISHABLE_KEY="pk_test_..."
CLERK_SECRET_KEY="sk_test_..."

# ── Server ───────────────────────────────────────────────────────────────────
# The Next.js web app origin (CORS allow-list).
WEB_URL="http://localhost:3000"
# Port this API listens on (defaults to 6969 if unset).
PORT="6969"
