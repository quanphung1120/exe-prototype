// `@repo/shared` — entity types, UI config and pure helpers shared by the
// Hono API (which owns the hardcoded records) and the Next.js web app (which
// fetches and renders them).

export * from "./types.js"
export * from "./config.js"
export * from "./helpers.js"
