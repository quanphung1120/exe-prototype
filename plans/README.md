# Implementation Plans

Generated by the improve skill on 2026-07-23, at commit `07f8908`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Upgrade Next.js past the 16.2.x middleware-bypass/SSRF advisories | P1 | S | — | DONE (commit `bdc2155` on branch `advisor/001-next-security-patch`, not merged) |
| 002  | Add a GitHub Actions CI gate for both apps | P1 | S | 001 (soft — so CI is green on first run) | DONE, merged to master (`1f665d8`, fast-forward); unverified on GitHub until pushed |
| 003  | Make discount usage-limit enforcement atomic | P1 | S | — | DONE (commit `8366112` on branch `advisor/003-discount-usage-race`, not merged; touched 4 files not 3 — plan's own Step 3 explicitly permitted extending `payments-service.test.ts`, judged on merit) |
| 004  | Rate-limit and size-cap the AI chat route | P1 | S | — | DONE, merged to master (`ee6acd3` via merge commit `a6ede64`) |
| 005  | Harden reschedule status check and IPN amount verification | P2 | S | — | DONE (commits `f619c39`, `a6ffc7b` on branch `advisor/005-payments-hardening`, not merged) |
| 006  | Expire Stream Chat user tokens | P2 | S | — | DONE (commit `9dde3e4` on branch `advisor/006-stream-token-expiry`, not merged) |
| 007  | Index hot venue reads; stop over-fetching for player-only accounts | P2 | M | — | DONE (commits `68925e2`, `809c2ee`, `dcdf799` on branch `advisor/007-venue-query-perf`, not merged; touched 2 files outside declared scope — `player.schema.ts`/`assessment.schema.ts` — to add explicit `@Prop({ type: ... })` annotations, a pre-existing tsx/esbuild DI-metadata gap that blocked the plan's own mandated test; verified as additive/behavior-preserving and consistent with the same fix already applied elsewhere in the codebase, e.g. `booking.schema.ts`, `profile.schema.ts`) |
| 008  | Stand up a web test baseline (Vitest) | P2 | M | 002 (CI wiring), 004 (rate-limit tests, soft) | DONE, merged to master (`8d5ced7`) — re-run as `advisor/008-web-test-baseline-v2` after 002+004 landed; now includes rate-limit tests + `pnpm test` wired into the CI web job (2 test files, 32 tests) |
| 009  | Move AI chat logic from web into the NestJS api (official AI SDK) | P2 | M | — | DONE, merged to master (`--no-ff` merge commit `7376f86`, branch `advisor/009-move-ai-chat-to-backend`, commits `dfe329d`, `adcd38d`, `cdcb1e6`; reviewer-verified all gates green — api typecheck/lint/build + 278 tests, web typecheck/lint + 32 tests). Two judged-on-merit deviations: (1) `api/pnpm-workspace.yaml` +1 line (`'@openrouter/sdk': false` build-script approval — same install-artifact class as the allowed lockfile change, consistent with existing `@clerk/shared`/`stream-chat` entries); (2) the `bookedSlots` prototype comment updated from a stale "Vercel/Prisma/Neon" reference to "multiple api instances / Mongo" (comment-only). Ported logic (SYSTEM prompt + all 5 tools) diff-verified byte-identical modulo indentation. Types use official AI SDK exports — no `any`/`never`/`unknown`. **Operator action:** relocate the real `OPENROUTER_API_KEY` from `web/.env.local` to `api/.env` and rotate it (key was exposed in web env).) |
| 010  | Make the AI chat call the api directly from the browser (delete the web proxy) | P2 | S | 009 | DONE, merged to master (`--no-ff` merge `40ef1c4`, branch `advisor/010-ai-chat-direct-to-api`, commits `87dbdb7`, `9db2b53`; pushed to origin 2026-07-23 — the push also published the previously local advisor merges 003/005/006/007/009; reviewer-verified all gates — api typecheck/lint/build + 283 tests incl. 5 new rate-limit tests, web typecheck/lint/build + 27 tests, no-web-api-routes + grep criteria green. Two judged-on-merit deviations: (1) Step 5 uses `useMemo(() => new DefaultChatTransport(...), [getToken])` instead of the plan's `getTokenRef`+`useState` pattern — the plan's pattern trips `react-hooks/refs` lint, and reviewer verified in `@ai-sdk/react` source that `useChat` re-reads `options.transport` per render via `latestRef` and only recreates `Chat` on `chat`/`id` change, so transport identity churn is safe; (2) `ai-native-dashboard.tsx` was Prettier-formatted whole-file (undocumented in executor report) — reviewer proved byte-identical to the repo's own formatter output on the base file, behavior-neutral. **Not run:** the live browser streaming/CORS/429 smoke test (no `.env`/Docker-WSL in worktree) — operator should `docker compose up --build` after merging and confirm `POST localhost:6969/api/ai/chat` streams and the 11th message in a minute yields the Vietnamese 429.) |

| 011  | Replace the hand-rolled AI-chat limiter with a per-user ThrottlerGuard subclass (ai chat + payments checkout) | P2 | S | 010 | DONE, merged to master (`--no-ff` merge `1f24bad`, commit `263897f`; reviewer-verified all gates in the worktree — api typecheck/lint + 283/283 tests, scope exactly the 6 in-scope files. Two judged-on-merit deviations, both documented by the executor: (1) `getTracker`/`throwThrottlingException` written as sync-signature methods returning `Promise.resolve`/`Promise.reject` instead of the plan's `async` — required by this repo's `require-await`/`no-unused-vars` lint rules, runtime behavior identical (the base guard `await`s both); (2) the done-criterion `grep "@Throttle(" src/` has 1 hit, but it's the warning *doc-comment text* inside `user-throttler.guard.ts` copied from the plan's own template — no actual library-decorator usage) |
| 012  | Promote UserThrottlerGuard to a global layer: per-IP + per-user (120/min shared bucket) on every authenticated route | P2 | S | 011 (merged) | DONE, merged to master (`--no-ff` merge `6b3e777`, commit `d62879b`; pushed to origin 2026-07-24 along with the 011 merge; reviewer-verified all gates — api typecheck/lint + 285/285 tests (7 in the guard file), provider order ThrottlerGuard→ClerkAuthGuard→UserThrottlerGuard confirmed, commit scope exactly the 5 in-scope files. Process note: the executor's resumed run operated in the MAIN repo (SendMessage resume lost the worktree context after a stale-snapshot STOP) — it stashed/restored the operator's uncommitted files correctly and committed only in-scope files to the advisor branch; reviewer restored the master checkout afterward. One plan-authoring bug found in review: the done-criterion `grep -rn "UseGuards" src/features/` ignores the pre-existing, unrelated `@UseGuards(RolesGuard)` in admin.controller.ts — the intent (no UserThrottlerGuard route registrations) holds; verified with the narrower grep. Operator smoke test after merge still pending, see plan's Maintenance notes) |
| 013  | Admin can manage discount codes (coupons): list/create/edit/toggle/delete via `/api/admin/discounts` + `/dashboard/admin/discounts` | P2 | M | none (prereq in-flight work landed as `44b8921`) | DONE (branch `advisor/013-admin-discount-management`, commits `b1a63bd`/`90a906b`/`92f9438` off `44b8921`, **not merged** — reviewer-verified all gates in the worktree: api typecheck/lint + 300/300 tests (9 new), web typecheck/lint 0 errors/build/27 tests, scope exactly the 14 in-scope files, i18n 49-key parity both locales. One judged-on-merit deviation, documented by the executor: `pnpm format` in web reformatted ~30 unrelated files (pre-existing prettier drift) and the executor reverted them to honor the scope criterion, re-running all gates after. Known cosmetic gap for follow-up: the usage cell hardcodes the Vietnamese literal "/người" instead of an i18n key; known deferred semantics per the plan: PATCH cannot clear an optional field (send-`null`), so e.g. converting a percent code carrying `maxDiscount` to fixed is rejected until that lands) |
| 014  | Community chat: message the venue after a paid booking, Clerk-backed user search, DMs + named group chats | P2 | L | none | DONE (branch `advisor/014-community-chat`, commits `c787abe` api / `f012453` web off `4e46141`, **not merged** — reviewer re-ran all gates in the worktree: api typecheck/lint/build + 314/314 tests (12 new in `stream-community.test.ts`), web typecheck/lint (0 errors)/build + 27/27 tests, all three routes present, i18n parity `[] []`, scope clean. Two judged-on-merit deviations, both documented: (1) `web/features/chat/venue-inbox-context.ts` extracted as its own file — explicitly authorized by the plan's Step 9 escape hatch (chat.tsx→channel-list.tsx import cycle); (2) `stream-service.test.ts` `makeService` helper extended with the new constructor fakes — required or its existing tests break, file was in scope. Known criterion caveat: the `helpers.ts` byte-sync diff is non-empty due to a PRE-EXISTING one-line comment drift on master (verified independently by the reviewer at `4e46141`); the executor's Prettier pass actually removed the other pre-existing wrap difference. Live browser smoke test NOT run (no env credentials in the worktree) — operator should verify venue chat/user search/DMs against real Stream+Clerk after merging) |

| 015  | Redesign the community chat page — real avatars in the history, one `ChatAvatar` treatment, flat chrome + mobile single-pane flow | P2 | M | none (extends landed 014 surface) | DONE, **merged to master** (`--no-ff` merge `346237d`, branch `advisor/015-chat-redesign-avatars`, commits `71bfd26` api / `b9253d5` web / `06ebde4` web-mobile; operator-requested merge 2026-07-24, redundant local `chat.tsx` hunk discarded pre-merge as planned — reviewer re-ran all gates in the worktree: api typecheck/lint + 316/316 tests (3 new seeding tests), web typecheck/lint 0 errors + 27/27 tests, all done-criteria greps green, scope exactly the 12 in-scope files. Two reviewer-driven revision rounds, both verified: (1) header `px-4 py-3` per spec; (2) `useCallback`/`useMemo`-stabilized `MobilePaneContext` callbacks — the plan's own original snippet caused `InitialChannel`'s effect to re-run every render, bouncing the mobile back button on `?channel=` deep links; plan file corrected to match. Step 7 (mobile single-pane flow + `Chat.backToChats` i18n key) was operator-added scope mid-execution. Live browser smoke test NOT run (no env creds in worktree) — operator should verify avatars backfill + mobile flow now that it's merged) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 002 after 001: so the first CI run is green (001 changes only web deps).
- 008 after 002: it extends the CI workflow's web job; after 004 (soft): the
  rate-limit module is one of its test targets (skippable if 004 hasn't run).
- 003/004/005/006/007 are independent of each other and can run in any order
  or in parallel worktrees.
- 009 (added 2026-07-23 at commit `9c8d129` via `/improve plan`) is
  independent of the rest. It relocates the AI chatbot's LLM/tools/prompt from
  `web/app/api/chat/route.ts` into a new `ai` feature in the api and turns the
  web route into a thin streaming proxy. Keeps the per-user rate limiter (plan
  004) in the web proxy, so it composes with 004 rather than superseding it.
- 010 (added 2026-07-23 at commit `7376f86` via `/improve plan`) requires 009:
  it deletes the web proxy that 009 left behind — the browser calls
  `POST /api/ai/chat` directly (Clerk Bearer via a `DefaultChatTransport`
  headers function, first browser→api call in the app). It **relocates** plan
  004's per-user rate limiter (module + tests) from `web/app/api/chat/` into
  the api's `ai` feature — after 010, plan 004's web-side artifacts no longer
  exist, and the web test suite added by 008 shrinks accordingly (the
  rate-limit tests move to `api/test/`).

- 011 (added 2026-07-23 at commit `40ef1c4` via `/improve plan`) requires 010:
  it deletes `api/src/features/ai/chat-rate-limit.ts` (the module 010 moved
  into the api) and replaces it with a route-scoped
  `UserThrottlerGuard extends ThrottlerGuard` in `api/src/common/`, keyed on
  Clerk userId via a custom `@UserThrottle()` decorator (NOT the library's
  `@Throttle()` — that metadata is also read by the global per-IP guard).
  Applied to `POST /api/ai/chat` (10/min, behavior-preserving) and
  `POST /api/payments/checkout` (operator-selected; `ipn` and `/stream/token`
  deliberately excluded). Supersedes the rate-limit module + test that plans
  004/010 produced; test count stays 283 (5 old tests replaced by 5 guard
  tests).

- 012 (added 2026-07-23 at commit `263897f` via `/improve plan`) requires 011
  **merged to master** — it edits `user-throttler.guard.ts` (created by 011)
  in place. Registers the guard as a third global `APP_GUARD` after
  `ClerkAuthGuard` (order load-bearing), default 120/min per user in ONE
  shared bucket across routes (operator chose parity with the per-IP limit);
  `@Public()` routes skipped via `shouldSkip`; `@UserThrottle` routes keep
  their own per-route buckets/limits and bypass the global budget (accepted);
  removes the now-redundant route-level `@UseGuards` (double-count bug
  otherwise). Unit tests can't exercise Nest DI — operator smoke test after
  merge is listed in the plan's Maintenance notes.

- 013 (added 2026-07-24 at commit `6b3e777` via `/improve plan`) has no plan
  dependencies, but it was planned against a **dirty working tree**: the
  operator has uncommitted changes to `admin.controller.ts`, `admin.dto.ts`,
  `discounts.service.ts`, `discount-code.schema.ts` and an in-flight
  `venues` → `venue-workspace` controller move. Commit/land that work before
  dispatching an executor — the plan's drift check makes an executor STOP if
  those files are still uncommitted. New surface: `GET/POST/PATCH/DELETE
  /api/admin/discounts[/:code]` on `AdminController` (delegating to new
  admin methods on `DiscountsService`; `AdminModule` gains `DiscountsModule`)
  plus a `/dashboard/admin/discounts` panel (nav entry, view, server
  actions, `AdminDiscounts` i18n in both locales). Deliberate semantics:
  `code` immutable and `usedCount` untouchable (payments reference codes by
  string), hard delete only when `usedCount === 0` (deactivate is the retire
  path), player validate/checkout paths untouched.

- 014 (added 2026-07-24 at commit `a92df40` via `/improve plan`) has no plan
  dependencies — it extends the landed Stream Chat integration (including
  006's token expiry, already on master). Three new capabilities in the
  existing `stream` feature: `POST /api/stream/venue-chats` (player ↔
  `venue.ownerId` channel, deterministic per user–venue id, gated on a
  paid/refunded booking at that venue; entry buttons on the bookings
  calendar + payment-success screen; operator inbox at
  `/dashboard/venue/[venueId]/messages` filtering on a new `venueId` custom
  channel field), `GET /api/stream/users/search` (first Clerk backend user
  lookup — `createClerkClient` behind a `CLERK_CLIENT` DI token; partial
  name search, **exact-only** email search so emails can't be harvested;
  `@UserThrottle` 30/min), and `POST /api/stream/conversations` (DMs with
  deterministic pair-hash ids + named group chats; create-only v1). Also
  adds `Booking.venueId` to both hand-duplicated shared copies.
  **Planned against a dirty tree**: the operator has uncommitted edits to
  `web/messages/{en,vi}.json` and `web/features/chat/ai-native-dashboard.tsx`
  (both near/in scope) — land or stash that work before dispatching an
  executor; the plan's drift check will STOP otherwise. StreamModule must
  register the Venue/Booking *schemas* directly, never import
  VenuesModule/BookingsModule (venues.module already imports StreamModule —
  cycle).

- 015 (added 2026-07-24 at commit `6bbbdc9` via `/improve plan`) has no plan
  dependencies — it restyles the `/dashboard/chat` surface 014 built (also
  used by the operator venue inbox). Two workstreams: (a) avatars — a new
  shared `web/features/chat/chat-avatar.tsx` renders the Stream user `image`
  with a deterministic gradient-initials fallback and replaces the four
  hand-rolled initials blocks (message history, channel list, header,
  new-chat dialog); demo players get DiceBear image URLs and `seedForUser`
  moves the `upsertUsers` call outside the seed marker so existing users
  backfill on token refresh; (b) visual — flat (de-carded) header/composer,
  bubble corner tails, richer empty state, online AvatarBadge dots.
  Reconciled at dispatch time (2026-07-24): the operator's uncommitted
  ChatShell de-carding is NOT required as a precondition anymore — the plan
  targets the committed state at `6bbbdc9` and includes the identical
  de-carding as Step 6 item 1; the operator's redundant local `chat.tsx` hunk
  must be discarded/stashed before merging the branch.

## Findings considered and rejected / already resolved

- **Refund-queue operator UI** (direction suggestion): **already built** —
  `web/features/admin/refunds.tsx` + `POST /api/admin/refunds/:bookingId/settle`
  (`admin.controller.ts:74`), and the venue workspace surfaces its queue via
  `venue-data-provider.tsx`/`reservations.tsx`. The audit suggestion was stale.
- **Cross-court self-overlap race** (`bookings.service.ts:1064-1077`): the
  code documents it as an accepted edge case ("a user racing two holds on
  different courts"). Recorded as a product decision; a global per-user lock
  isn't worth the hot-path contention unless abuse appears.
- **Sweeper N-instance duplication**: correct (transitions are atomic and
  idempotent via `trySetStatus`), just wasteful with multiple api instances.
  Revisit only when horizontally scaling — a short-TTL advisory lock is the
  known fix.
- **Hand-duplicated `web/lib/shared/` vs `api/src/shared/`**: deliberate
  (2026-07-09 de-monorepo decision) and currently in sync byte-for-byte
  (modulo `.js` import extensions). Plan 008's mirrored tests + a possible CI
  drift-diff job are the guards; not a finding.
- **God files** (`session.tsx` 2190 ln, `bookings.service.ts` 1432 ln,
  `schedule.tsx` 1530 ln): real debt but L-effort/MED-risk decomposition; not
  planned this round — do it behind characterization tests, feature by
  feature, when one of them next needs substantive change.
- **`confirmSlaMinutes` string-env coercion** (`bookings.service.ts:205-210`):
  only misbehaves on a malformed env value; fold a `Number.isFinite` guard in
  whenever that file is next touched.
- **Room capacity/join check-then-act races** (`rooms.service.ts:109-129,
  188-199`): real but M-effort (array-length invariants are awkward to express
  atomically in Mongo) and low-frequency; deferred — candidate for the next
  planning round via a per-room lock doc mirroring `withCourtLock`.
- **`ipnPayload` stored verbatim (PII)** (`payments.service.ts:284`):
  deferred data-minimization follow-up noted in plan 005.
- **Unused `@fullcalendar/*` deps + no `next/dynamic` for mapbox/three**:
  small true findings; not planned this round — trivial enough to fold into
  any web-touching change (drop the four packages; wrap `CourtMap` and the
  3D matchmaking view in `next/dynamic({ ssr: false })`).
- **Stale root docs** (`TODO.md` shipped-but-open, `STREAM_CHAT_PLAN.md` done
  plan with a stale warning): housekeeping — archive under `docs/` at will;
  no plan needed.
- **Not audited this run**: `docs/superpowers/`, `VienTD-Review/` contents,
  the GSAP landing animations, and deep Mapbox/three rendering code.
